{
  "$comment": [
    "Reviewed npm-audit allowlist (P1 W6-T4, ADR-0016 D16 pipeline).",
    "npm audit has no native ignore file, so CI runs `npm audit --json` and pipes",
    "it through scripts/npm-audit-gate.mjs, which FAILS on any advisory at or above",
    "the configured severity floor (high) whose GHSA id is NOT listed below.",
    "This mirrors the reviewed .trivyignore convention: never a blanket ignore —",
    "every entry has a justification + an expiry (re-review) date. An entry past",
    "its expiry makes the gate fail until it is re-reviewed and renewed or removed.",
    "Triage policy: docs/security/supply-chain-scanning.md."
  ],
  "severityFloor": "high",
  "allow": [
    {
      "ghsa": "GHSA-hmw2-7cc7-3qxx",
      "package": "form-data",
      "via": "transitive (dev-only: @testing-library / jsdom test toolchain)",
      "severity": "high",
      "advisory": "form-data CRLF injection via unescaped multipart field names",
      "justification": "Present only in the dev/test dependency tree (not shipped in the vite production bundle). Fix is a transitive bump pending upstream test-tooling release. Not part of the deployed artifact; no runtime exposure.",
      "expires": "2026-09-30"
    }
  ]
}
