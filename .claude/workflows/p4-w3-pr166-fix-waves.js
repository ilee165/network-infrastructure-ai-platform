export const meta = {
  name: 'p4-w3-pr166-fix-waves',
  description: 'PR #166 review remediation: 4 sequential fix waves (CI blockers, evidence correctness, security, perf/operability)',
  phases: [
    { title: 'F1 CI blockers', detail: 'claim race + WeasyPrint 68', model: 'fable' },
    { title: 'F2 evidence correctness', detail: '7 correctness fixes' },
    { title: 'F3 security', detail: '6 security/boundary fixes', model: 'fable' },
    { title: 'F4 perf/operability', detail: '7 perf + observability fixes' },
  ],
}

const REPO = 'D:/Multi-Agent workflow/network-infrastructure-ai-platform'

const COMMON = `
Repo: ${REPO}, branch feat/p4-w3-compliance-reporting (verify git branch --show-current; do NOT push, do NOT amend/rebase existing commits).
Gates before committing (from backend/ using .venv/Scripts/python.exe): ruff check . && ruff format --check . && mypy && lint-imports (PYTHONUTF8=1) && pytest. Redirect pytest to a file and read the raw tail — rtk one-line summaries lie. Unit suite is SQLite; if your fix has PG-semantic behavior add/extend tests under backend/tests/pg/ WITH "pytestmark = pytest.mark.integration" (mandatory — the blocking pg-integration job silently deselects files without it). Two known LOCAL-only failures unrelated to this work may appear in full runs (tests/pg/test_app_db_wiring_pg.py::test_unset_reader_url_reuses_primary_engine_and_reads_committed_write, tests/plugins/test_panos_conformance.py::TestPanosCredentialHygiene::test_api_key_not_logged) — they are green in CI; do not chase them, exclude from your pass/fail judgment.
graphify-out/graph.json exists — run 'graphify query "<question>"' to orient before reading raw source files.
Stay STRICTLY inside the enumerated findings. TDD: write/extend the failing test first where a test is named. Exactly ONE atomic commit for the whole wave, conventional style "fix(reports): PR #166 wave <N> — <summary>". Return raw data: commit SHA, per-finding disposition, gate evidence.`

const VERDICT = {
  type: 'object',
  required: ['unresolved', 'commit'],
  properties: {
    commit: { type: 'string' },
    unresolved: { type: 'array', items: { type: 'string' } },
    notes: { type: 'string' },
  },
}

async function wave(title, fixPrompt, verifyList, model) {
  phase(title)
  const opts = { label: `fix:${title}`, phase: title, agentType: 'wf-fixer' }
  if (model) opts.model = model
  const fix = await agent(fixPrompt, opts)
  const vOpts = { label: `verify:${title}`, phase: title, agentType: 'wf-verifier', schema: VERDICT }
  const verify = await agent(`Verify the newest commit on branch feat/p4-w3-compliance-reporting in ${REPO} (git log -1) genuinely resolves each finding below. Read-only; read the actual diff and code, do not trust the fixer's claims. Report the commit SHA and only findings that remain unresolved.\n\nFindings:\n${verifyList}\n\nFixer report: ${JSON.stringify(fix).slice(0, 3000)}`, vOpts)
  log(`${title}: unresolved=${verify?.unresolved?.length ?? '?'}`)
  return { fix: typeof fix === 'string' ? fix.slice(0, 1500) : fix, verify }
}

const F1_FINDINGS = `
1. CLAIM RACE (CI-red, tests/pg/test_reports_pg.py::test_concurrent_claims_yield_exactly_one_row fails with UniqueViolationError on uq_report_runs_kind_period). backend/app/workers/tasks/reports.py ~line 164: INSERT ... ON CONFLICT (id) DO NOTHING. PostgreSQL only suppresses conflicts on the ARBITER index; under true concurrency the sibling unique index uq_report_runs_kind_period (migration 0020) can trip first and raise. The deterministic id (engines/reports/idempotency.py) means id-conflict iff natural-key-conflict, but arbiter choice does not make the race safe. Fix: wrap the claim INSERT in a SAVEPOINT (session.begin_nested) catching IntegrityError, then re-select and classify the existing row — handles ALL unique conflicts deterministically. Keep the existing FAILED-reclaim and SUCCEEDED-skip classification.
2. RESUME WITHOUT OWNERSHIP (Critical). Same file ~lines 168-194: a row in RUNNING status unconditionally returns "resumed" with no ownership token, heartbeat, or claim-age check — two simultaneous requests both generate: duplicate report.generated audit entries (line ~357) and interleaved artifact delete+insert (line ~340). Fix: add claim ownership (e.g. claimed_by token + claimed_at already exist or add columns via new migration ONLY if necessary — prefer using existing columns; check the model first) and resume ONLY a stale claim (age > configurable timeout) or terminal states; the loser of an active claim returns a non-generating status. Extend tests/pg/test_reports_pg.py: concurrent-claim test must assert exactly one generator AND cover the active-claim-loser path. Keep test green under real PG (run locally if PG available via the repo's pg test harness; otherwise state clearly it needs CI).
3. WEASYPRINT (CI-red: pip-audit PYSEC-2026-2034/PYSEC-2026-3412, Trivy HIGH CVE-2025-68616 SSRF, all fixed in 68.0). backend/pyproject.toml:89 pins "weasyprint>=63,<66"; backend/requirements.lock.txt locks 65.1. Fix: raise constraint to >=68,<69, regenerate the lockfile using the repo's documented lock procedure (find it — check scripts/, docs, or the lockfile header for the generating command; drift gate 'lockfile' must stay green), and re-run the render tests. The pyproject comment (lines ~85-88) warns evidence artifacts must not silently re-render under a major bump: if PDF golden/contract expectations change under 68, update them DELIBERATELY and record what visually/structurally changed in the commit message. Also verify backend/app/engines/reports/render.py _deny_all_url_fetcher still behaves (it delegates to default_url_fetcher after check_fetch_url — the SSRF fix in 68 is upstream of us; our deny-first check stays).`

const F2_FINDINGS = `
1. PARTIAL-DAY FALSE GAP. backend/app/engines/reports/audit_integrity.py: _load_runs selects closed-open on started_at >= period_start (~191-193) excluding valid runs on the partial first day, while _period_days (~204-211) enumerates from start.date() midnight — period [07-01T12:00Z, 07-02T12:00Z) falsely reports July 1 as GAP + finding (~289-295). Fix: assess only complete UTC days (or load runs on UTC-day boundaries for the daily-evidence section) while keeping the requested window for run-detail rows. SIBLING: backend/app/engines/reports/compliance_posture.py _period_days (~225-232, loop ~314) has the identical shape for trend GAP rows — fix both. Tests: partial-boundary-day cases for both engines.
2. LIFECYCLE SEQ TIE-BREAK. backend/app/engines/reports/change_report.py:263 orders by (AuditLog.created_at, AuditLog.id); AuditLog.seq exists and backend/app/models/audit.py (~118-146) explicitly warns chain order is seq, NOT (created_at, id) — UUID tie-break can invert order and poison _executors "last claim wins" (~286-296). Fix: order by created_at, then seq (nulls_last like access_review.py:244 does), id as final fallback. Test: two same-timestamp transitions assert deterministic executor.
3. STRANDED RUNNING. backend/app/workers/tasks/reports.py ~337-373: Phase-3 (artifact delete/insert, status update, audit, commit) sits outside any try/except unlike Phases 1-2, and no autoretry_for on the tasks (~392,406) — an ordinary persistence exception leaves the claim RUNNING forever. Fix: wrap Phase-3 with typed handling that rolls back and routes to _fail_run (making the run reclaimable), preserving artifact-consistency; add a test injecting a Phase-3 DB failure asserting the run lands FAILED and is reclaimable.
4. BEAT-SLOT IDENTITY. Same file ~407-416: beat message carries only kind; worker computes scheduled_period(cadence, datetime.now(UTC)) at EXECUTION time — a redelivered/delayed tick crossing UTC midnight derives the next period's id and the intended period is permanently ungenerated. Fix: compute the slot bounds at DISPATCH (beat schedule entry / sender) and carry period_start/period_end in the message; worker uses message bounds, falling back to now() only for legacy empty messages. Test: delayed-tick-across-boundary case.
5. GRANT-CHECK TXN POISON. backend/app/services/audit/verify_job.py ~243-250: check_audit_log_grants runs on the same session, no savepoint; on statement failure the except-continue path runs _checkpoint_state SELECT + history INSERT on the aborted txn — they raise, outer except (~342-343) swallows, and the honest UNAVAILABLE row never persists. Fix: run the grant check in begin_nested() (or rollback in the except before continuing) so the UNAVAILABLE outcome IS persisted. Test: a RAISING grant check (not just SQLite-dialect skip) asserting the unavailable history row lands — current tests only cover non-raising paths.
6. UNEVALUATED DEVICES VANISH. backend/app/workers/tasks/reports.py ~529-530,550,555: sweep skips snapshot-less devices with bare continue — excluded from device_scope, no finding; backend/app/engines/reports/compliance_posture.py ~283-299 derives device rows solely from finding keys, out-of-scope section (~330-334) covers vendors only — 1-of-100 evaluated renders as a clean estate. Fix: record supported-but-unevaluated devices explicitly (in device_scope/payload) and surface them as coverage gaps in the report (not silently absent). Test: mixed evaluated/unevaluated estate asserts explicit unevaluated rows/count.
7. ACCESS-REVIEW LABEL. backend/app/engines/reports/access_review.py ~254-262: _classify receives only window_start; an account created AFTER period_end with no login gets CLASS_NEW_ACCOUNT ("created inside the window", line ~18 definition). Do NOT exclude the account — the roster is documented generation-time state (~20-23, NOTE_SCOPE ~132-136). Fix label only: pass period_end and classify created_at >= period_end distinctly (e.g. post-period account) or as its honest state. Test: post-period account gets the new honest label.`

const F3_FINDINGS = `
1. EXISTENCE DISCLOSURE. backend/app/agents/framework/read_facade.py ~444-450: run queried by id BEFORE RBAC, then raises ReportAccessDeniedError post-fetch — sub-floor caller distinguishes None (missing) vs denied (exists), and run ids are computable offline via deterministic_run_id; module's own comment (~384-385) says existence is RBAC-scoped (ADR-0053 §3). Fix: filter the query to kinds visible to the role; return None for unauthorized. Check the API layer 403-vs-404 seam (backend/app/api/v1/reports.py ~180-181) — same class; align it (404 for both missing and forbidden at the API, or document why not). Tests both layers.
2. PASSWORD-HASH PROJECTION. backend/app/engines/reports/change_report.py ~276-283: select(User) drags users.password_hash into the report-engine session, contradicting the module's §6 claim (~12-16); access_review.py ~186-194 already projects columns. Fix: project only User.id/username/idp_subject. ALSO: the _DENY_COLUMNS boundary test (backend/tests/engines/reports/test_boundary.py ~57,177-182) is VACUOUS for change_report — empty test DB early-returns before SQL (~279-281). Make that test actually execute _load_identities SQL (seed a user + identity refs) so the deny-column assertion bites.
3. APPEND-ONLY PARITY FOR VERIFICATION HISTORY. backend/alembic/versions/0021_p4_audit_chain_verification_runs.py (~38-59) creates the 7-year evidence table with NO append-only protection, unlike audit_log (0001 line ~222 REVOKE UPDATE/DELETE FROM PUBLIC) and approvals (0009 ~44-58 trigger). A role with UPDATE on app tables rewrites break→clean undetected; audit-integrity report explicitly trusts persisted rows. Fix: NEW migration (0022) adding REVOKE UPDATE, DELETE + an append-only trigger mirroring the 0009 pattern. Test: PG tamper test under backend/tests/pg/ (with pytestmark = pytest.mark.integration) proving UPDATE/DELETE are refused — must BITE (assert the refusal error, not just absence of change).
4. AGENT DURABLE AUDIT. backend/app/agents/documentation/tools.py ~1245-1264: agent path goes ensure_report_access → args → send_task with zero audit.record; HTTP path commits report.generation_requested BEFORE dispatch (backend/app/api/v1/reports.py ~105-117). Fix: record the same durable generation_requested event (committed) before enqueueing on the agent path, attributed to the requesting principal; assert it in the agent tool test.
5. PERIOD SPAN CAP. backend/app/schemas/reports_api.py ~25-48 validates ordering/future only — year-1→today accepted; builders materialize one tuple per day (~739K days; audit-integrity also per-gap-day rows). Fix: enforce a justified max span (e.g. 400 days — covers annual reports + slack; state rationale in a comment) AND a sane period_start floor, in BOTH the API schema and the facade path read_facade.report_generation_args (~478-484) — the worker path executes what these produce. Boundary tests both entry points (at-cap ok, over-cap rejected 422/ValueError).
6. RENDER-PATH AST SCANNER. backend/tests/engines/reports/test_boundary.py ~76-80: renderer scanner regex misses "from app.engines.reports import render; render._render_pdf(...)". The deny-import half (~104-117) is already backstopped by the blocking lint-imports contract — leave it. Fix ONLY the render-path leg: resolve imports+calls via AST so aliased/module-level access to the render internals is caught. Prove it bites: temporarily plant a violating fixture string in the test to assert the scanner catches the aliased form.`

const F4_FINDINGS = `
1. ARTIFACT BLOB DEFERRAL. ReportArtifact.content is plain non-deferred LargeBinary (backend/app/models/reports.py ~122); facade metadata reads select full entities (backend/app/agents/framework/read_facade.py ~420,~452 — NOTE its AST purity guard permits only bare select, so fix at the MODEL with deferred()), and API _get_run_or_404 selectinload(ReportRun.artifacts) (backend/app/api/v1/reports.py ~81) loads every sibling's bytes for metadata GET and downloads. Fix: deferred() on content + download route loads the ONE requested artifact's content explicitly (async-safe — no implicit lazy-load, MissingGreenlet hazard). Tests: metadata path emits no content column (assert via SQL capture or inspect), download still streams bytes.
2. SWEEP N+1. backend/app/workers/tasks/reports.py ~517-528: per-device latest-ConfigSnapshot query in loop. Fix: one bounded bulk query (DISTINCT ON (device_id) ... ORDER BY device_id, captured_at DESC, created_at DESC or window function; mind SQLite unit-suite compat — use a dialect-portable form or route through PG test). Preserve device_ids/finding_count behavior.
3. ASYNC DISPATCH. backend/app/api/v1/reports.py ~118: bare celery_app.send_task in async route. Fix: wrap via the repo's established pattern (loop.run_in_executor like backend/app/agents/documentation/tools.py ~1257-1263). ONLY this route — sibling routes (agents.py, discovery.py) are pre-existing class, out of scope (noted for W4).
4. PREFORK METRICS. Metrics server starts on worker_init in the parent serving default REGISTRY (backend/app/workers/celery_app.py ~357-364); task-body mutations happen in prefork children (compose -c 2) → never visible at :9808. Fix: prometheus_client multiprocess mode — PROMETHEUS_MULTIPROC_DIR + MultiProcessCollector in the worker metrics server, correct gauge semantics for last-success (multiprocess mode='max' or 'mostrecent'), Settings field for the dir if config needed (then regenerate .env.example via backend/scripts/generate_env_example.py --write — config-drift gate), compose/helm env wiring. Test: prefork task-to-scrape test if feasible; minimum a unit test that the collector registry uses multiprocess when the dir is set.
5. ALERTS DEPLOYED BY NOTHING. deploy/observability/report-engine.alerts.yaml is promtool-validated in CI but no chart/compose references it. Fix: opt-in PrometheusRule template mirroring deploy/kubernetes/netops/templates/slo-burn-rate-prometheusrule.yaml (same observability.prometheusRule.enabled flag, inlined rule body, single-source-of-truth comment convention), + rendered-chart assertion test (the infra gates run helm lint/kubeconform — keep green).
6. NEVER-SUCCEEDED SILENCE. Same alerts file (~37-47,62-65): bare (time() - gauge) > N returns no series when gauge never set; gauge only set on success (backend/app/core/metrics.py ~416, workers/tasks/reports.py ~377). Fix: initialize the last-success gauge for all four kinds at worker startup to boot timestamp (existing expressions then fire naturally at cadence+grace) — coordinate with F4-4 multiprocess gauge semantics; update the alerts-file NOTE block; add promtool test case for the never-succeeded scenario in report-engine.alerts.test.yaml (must bite).
7. OPENAPI DOWNLOAD CONTENT-TYPE. backend/app/api/v1/reports.py ~185-191 download route lacks responses= declaration → docs/api/openapi.json (~9385) says 200 application/json {}. Fix: responses={200: {"content": {"text/csv": {...}, "application/pdf": {...}}}} (schema format binary) on the decorator, regenerate the OpenAPI file via the repo's contract-drift procedure (find the generator; the contract-drift CI gate re-derives it — must stay green).`

const r1 = await wave('F1 CI blockers', `Apply EXACTLY these validated PR #166 review findings (wave F1 — CI unblock).${COMMON}\n\nFINDINGS:\n${F1_FINDINGS}`, F1_FINDINGS, 'fable')
const r2 = await wave('F2 evidence correctness', `Apply EXACTLY these validated PR #166 review findings (wave F2 — evidence correctness).${COMMON}\n\nFINDINGS:\n${F2_FINDINGS}`, F2_FINDINGS)
const r3 = await wave('F3 security', `Apply EXACTLY these validated PR #166 review findings (wave F3 — security/boundary).${COMMON}\n\nFINDINGS:\n${F3_FINDINGS}`, F3_FINDINGS, 'fable')
const r4 = await wave('F4 perf/operability', `Apply EXACTLY these validated PR #166 review findings (wave F4 — perf/operability).${COMMON}\n\nFINDINGS:\n${F4_FINDINGS}`, F4_FINDINGS)

return {
  F1: r1, F2: r2, F3: r3, F4: r4,
  all_unresolved: [r1, r2, r3, r4].flatMap((r, i) => (r.verify?.unresolved || []).map(u => `F${i + 1}: ${u}`)),
}