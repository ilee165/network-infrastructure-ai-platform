# P4 W4 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. After T0A and T0B, execute T1 → T2A → T2 → T3 on the shared branch; T2A is a runtime-correction precondition and does not edit the evidence ledger.

**Goal:** Close P4 with deterministic, continuously biting evaluation gates and one release-readiness record at a single release HEAD.

**Architecture:** T0 first repairs the planning contract and the remaining W3 CSV-injection gap in two atomic commits. T1 adds vendor/plugin proof. W4-T2 preflight after T1 identifies bounded derivation contract defects, so T2A corrects them before the eval-only T2 authors its independent corpus; T3 adds report proof. T1, T2, and T3 record only non-self-referential task status, focused commands/results, bite test node IDs, and blocking-CI collection paths in their ledger sections; T2A does not edit the ledger. A bounded dependency-audit remediation follows T3. T4 audits the seven-commit pre-T4 candidate, owns the ledger's final lifecycle/status, and records each landed eval task commit SHA plus one final release HEAD and the blocking run/job URL and result for each suite before closing the branch as commit eight. T4 also records named deferrals, accepts ADRs 0050–0053 only if all gates are green, and synchronizes the roadmap markers.

**Tech Stack:** Python 3.12, pytest/pytest-asyncio, SQLAlchemy/PostgreSQL integration tests, FastAPI plugin/report/topology contracts, GitHub Actions, Markdown evidence and ADRs.

## Global Constraints

- Work from `7298f4b8` on `feat/p4-w4`; preserve unrelated user work in the original checkout.
- T0A, T0B, T1, T2A, T2, and T3 each produce one atomic task commit; the bounded dependency-audit remediation at `71cd249d` is a seventh pre-T4 commit, and T4 is the eighth/final commit.
- Execute T1 → T2A → T2 → T3 on the shared branch. T1, T2, and T3 each edit only their named section of `docs/roadmap/evidence/P4-W4-evals-evidence.md`; T2A edits no ledger section.
- T2A is the derivation-runtime correction precondition. T2 is eval-only and must not edit `backend/app/engines/topology/app_derivation.py`.
- T4 alone edits the ledger's top-level final-revalidation lifecycle/status and final table; it records each landed T1/T2/T3 task commit SHA, one common final release HEAD, and each suite's blocking run/job URL and result.
- T1–T3 negative controls use assert-red-inside-green tests in blocking CI, never temporary red commits.
- W4-T2 gates the curated corpus at precision `1.0` and recall `1.0`; contract-defined exclusions are not false negatives.
- Every new `backend/tests/pg/*.py` file declares `pytestmark = pytest.mark.integration` and is proven collectible with `-m integration --collect-only`.
- No live LLM or live F5/vCenter dependency is introduced into blocking CI.
- T4 names the bare `send_task` sweep, transactional report outbox, live lab, certified scale, 30-day soak, pentest, and break-glass cadence as deferrals with promotion paths.

---

### Task 0A: Planning and handoff contract

**Files:**
- Create: `docs/roadmap/p4-tasks/W4-T0-housekeeping.md`
- Create: `docs/roadmap/P4-W4-HANDOFF.md`
- Create: `docs/roadmap/evidence/P4-W4-evals-evidence.md` (explicitly pending task-local T1/T2/T3 sections plus T4-only final revalidation table)
- Modify: `docs/roadmap/P4-PLAN.md`
- Modify: `docs/roadmap/p4-tasks/README.md`
- Modify: `docs/roadmap/p4-tasks/W4-T1-plugin-conformance-cross-vendor-rerun.md`
- Modify: `docs/roadmap/p4-tasks/W4-T2-derivation-eval-corpus.md`
- Modify: `docs/roadmap/p4-tasks/W4-T3-report-conformance-evals.md`
- Modify: `docs/roadmap/p4-tasks/W4-T4-gate-evidence-readiness.md`

**Produces:** One authoritative W4 contract: the vendor/plugin conformance matrix grows while the nine-agent routing roster does not; T2 thresholds are 1.0/1.0; negative controls are assert-red-inside-green; the two dispatch findings are P5 deferrals.

- [ ] Add the formal T0 spec with separate T0A/T0B exit criteria and commit boundaries.
- [ ] Replace stale W3 D+/pending language with merged-green PR #166 state.
- [ ] Amend the handoff to record the owner decisions and exact durability guarantee.
- [ ] Correct W4-T1's matrix-versus-routing-roster wording; bind W4-T2 numeric thresholds; bind the common evidence mechanism and evidence path in T1–T4.
- [ ] Seed the ledger as explicitly pending: T1/T2/T3 sections accept only task status, focused commands/results, bite test node IDs, and blocking-CI collection paths, while T4 alone owns the top-level lifecycle/status and the table for landed task commit SHAs, one final release HEAD, run/job URLs, and results; make no unexecuted evidence claims.
- [ ] Run a focused stale-language/link scan and `git diff --check`.
- [ ] Commit as `docs(p4): establish W4 execution contract`.

### Task 0B: LF CSV-injection prefix

**Files:**
- Modify: `backend/tests/engines/reports/test_render.py`
- Modify: `backend/app/engines/reports/render.py`

**Produces:** `neutralize_cell()` prefixes LF-leading cells exactly like the other spreadsheet formula leads.

- [ ] Add `"\npayload"` to the dangerous-prefix parametrization and run the focused test to observe failure.
- [ ] Add `"\n"` to `_FORMULA_PREFIXES` and update the renderer contract comment.
- [ ] Run `pytest tests/engines/reports/test_render.py -q`, Ruff on both files, and `git diff --check`.
- [ ] Commit as `fix(reports): neutralize LF-prefixed CSV cells`.

### Task 1: Vendor conformance and routing no-regression

**Files:**
- Create or modify focused files under `backend/tests/agents/eval/fixtures/` and `backend/tests/agents/eval/`.
- Modify conformance helpers/tests under `backend/tests/plugins/` only where required for completeness checking.
- Modify blocking CI workflow only if the existing backend eval selection does not collect the new suite.
- Update only: `docs/roadmap/evidence/P4-W4-evals-evidence.md` (T1 non-self-referential task status/focused/bite-node/CI-collection-path section).

**Produces:** A vendor/capability matrix containing F5 BIG-IP and VMware, an unchanged nine-agent routing roster with new vendor-surface prompts, comparative P3-baseline assertions, and a monkeypatched missing `_INTERFACE_SPECS` bite proof.

- [ ] Write failing routing-roster/case/completeness tests first and prove the intended red results.
- [ ] Implement only the eval fixtures/helpers needed to make them green.
- [ ] Assert the missing `_INTERFACE_SPECS` mutation is rejected inside the passing suite.
- [ ] Run focused eval/conformance tests, Ruff, collection proof, and `git diff --check`.
- [ ] Before committing, record only task status, focused commands/results, bite test node IDs, and the blocking-CI collection path in T1's ledger section; T4 later records the landed T1 commit SHA and all final-release fields.
- [ ] Commit as `test(evals): extend P4 vendor conformance matrix`.

### Task 2A: Derivation contract corrections

**Files:**
- Modify: `backend/app/engines/topology/app_derivation.py`
- Modify: `backend/tests/engines/topology/test_app_derivation.py`
- Create: `docs/roadmap/p4-tasks/W4-T2A-derivation-contract-corrections.md`
- Modify: `docs/roadmap/P4-W4-HANDOFF.md`
- Modify: `docs/roadmap/P4-PLAN.md`
- Modify: `docs/roadmap/p4-tasks/README.md`
- Modify: `docs/roadmap/p4-tasks/W4-T2-derivation-eval-corpus.md`
- Modify: `docs/superpowers/plans/2026-07-20-p4-w4.md`
- Do not modify: `docs/roadmap/evidence/P4-W4-evals-evidence.md`

**Produces:** One bounded runtime-correction precondition for T2: a nondefault
F5 route-domain cannot reconcile a bare member IP against global inventory or
a VMware guest IP; default-domain variants still reconcile; FQDN evidence
remains independent; and VMware provenance retains the full virtual-server →
pool → member chain.

- [ ] Add focused regressions first and prove the nondefault F5 join, unsafe
  VMware join, and missing pool provenance fail while the guards remain green.
- [ ] Preserve stable member-name identity/provenance; parse the canonical
  member address and expose it as joinable evidence only for absent, empty, or
  `"0"` route domains.
- [ ] Add the pool step to VMware provenance without changing `_AddressIndex`,
  persistence schemas, migrations, or source precedence.
- [ ] Prove nondefault-domain FQDN matching still works and Tools-less VMs do
  not produce VMware placement edges.
- [ ] Run the focused derivation tests, adjacent store/PG tests where locally
  available, Ruff check/format, touched-module mypy, and `git diff --check`.
- [ ] Commit as `fix(topology): preserve route-domain and provenance contracts`.

### Task 2: Application-dependency derivation corpus

**Files:**
- Create deterministic corpus/expected-graph fixtures under `backend/tests/agents/eval/fixtures/`.
- Create eval helpers/tests under `backend/tests/agents/eval/`.
- Create a PostgreSQL write/idempotency test under `backend/tests/pg/` when persistence is exercised.
- Update only: `docs/roadmap/evidence/P4-W4-evals-evidence.md` (T2 non-self-referential task status/focused/bite-node/CI-collection-path section).

**Produces:** Contract-authored expected graphs for ADC, VMware, DNS, and manual tags; exact precision/recall scoring; provenance and impact checks; planted-wrong-edge and suppressed-source bite assertions.

- [ ] Start only after T2A lands; keep this commit eval-only and do not modify
  the production derivation module.
- [ ] Write scorer tests proving exact match is 1.0/1.0 and mutations reduce precision or recall.
- [ ] Author synthetic estate and expected graph independently from runtime output.
- [ ] Exercise all four sources, precedence/exclusions, provenance, impact directionality, and deterministic idempotency.
- [ ] Assert the gate rejects every score below 1.0 and accepts only exact curated-corpus matches.
- [ ] Run focused unit/PG tests, integration collection proof, Ruff, and `git diff --check`.
- [ ] Before committing, record only task status, focused commands/results, bite test node IDs, and the blocking-CI collection path in T2's ledger section; T4 later records the landed T2 commit SHA and all final-release fields.
- [ ] Commit as `test(evals): add exact application-derivation corpus`.

### Task 3: Report conformance and fail-closed evidence

**Files:**
- Create report corpus/structure fixtures under `backend/tests/agents/eval/fixtures/`.
- Create eval helpers/tests under `backend/tests/agents/eval/`.
- Create a PostgreSQL history/artifact assertion under `backend/tests/pg/` if required.
- Update only: `docs/roadmap/evidence/P4-W4-evals-evidence.md` (T3 non-self-referential task status/focused/bite-node/CI-collection-path section).

**Produces:** CSV/PDF structure checks for all four reports, completeness rules, deterministic artifacts, independent emitted-text secret scans, and assert-red-inside-green redaction mutations.

- [ ] Write failing structure/completeness/redaction tests first.
- [ ] Cover each report kind and extract structure rather than comparing raw PDF bytes.
- [ ] Prove deny-class and PEM secrets fail closed with typed error, no artifact, and metric increment; prove digest strings pass.
- [ ] Prove disabling the filter makes the independent artifact scan fail inside the passing suite.
- [ ] Include every CSV formula lead, including LF, and deterministic repeat rendering.
- [ ] Run focused unit/PG tests, integration collection proof, Ruff, and `git diff --check`.
- [ ] Before committing, record only task status, focused commands/results, bite test node IDs, and the blocking-CI collection path in T3's ledger section; T4 later records the landed T3 commit SHA and all final-release fields.
- [ ] Commit as `test(evals): add report conformance bite proofs`.

### Task 4: P4 release readiness

**Files:**
- Create: `docs/roadmap/P4-RELEASE-READINESS.md`
- Populate only the ledger's T4-owned final-release-HEAD revalidation table after auditing the T1/T2/T3-owned task-local sections: `docs/roadmap/evidence/P4-W4-evals-evidence.md`
- Modify: `docs/adr/0050-f5-bigip-plugin.md` through `0053-compliance-audit-reporting.md`
- Modify the ADR index.
- Modify: `docs/roadmap/PRODUCTION.md`
- Modify: `docs/roadmap/P4-PLAN.md`
- Modify: `docs/roadmap/p4-tasks/README.md`

**Produces:** One release-HEAD audit of the seven-commit pre-T4 candidate. The
T4-owned ledger lifecycle/status records each landed T1/T2/T3 eval task commit
SHA, one final release HEAD, and each suite's blocking GitHub Actions run/job
URL and result; every P4-scoped gate is evidenced or explicitly deferred;
ADRs are Accepted; roadmap markers are consistent; and the T4 closeout becomes
the eighth and final branch commit.

- [ ] Rebase/verify the pre-T4 combined HEAD contains T0A, T0B, T1, T2A, T2,
  T3, and the bounded dependency-audit remediation only beyond `7298f4b8`.
- [ ] Run the complete backend gates and all relevant CI-equivalent structural checks; capture exact results and HEAD.
- [ ] Revalidate T1/T2/T3 at the final combined release HEAD, update T4's top-level lifecycle/status, record that one final release HEAD, and populate each suite row with its landed task commit SHA plus blocking run/job URL and result; task-local pre-commit evidence is not final evidence.
- [ ] Record the dispatch guarantee: durable request audit commit first; then best-effort, untracked Celery publication that may enqueue duplicates; deterministic worker claim makes generation idempotent; requested-but-unclaimed crash/broker-uncertainty window; transactional outbox plus relay/recovery deferred to P5.
- [ ] Name all required deferrals and promotion paths.
- [ ] Accept ADRs and update indexes/roadmaps only after the evidence is verified.
- [ ] Run docs/link/status consistency checks and `git diff --check`.
- [ ] Commit as `docs(p4): close W4 release readiness`.

### Final verification

- [ ] Run `cd backend && .venv/bin/pytest -q` outside the filesystem sandbox if needed for `aiosqlite` worker threads.
- [ ] Run `cd backend && .venv/bin/ruff check . && .venv/bin/ruff format --check . && .venv/bin/mypy && .venv/bin/lint-imports`.
- [ ] Run frontend and repository drift/structural gates affected by the W4 commits.
- [ ] Run `graphify update .` if the graph artifact is available in the worktree; otherwise document that the clean worktree did not contain the untracked graph.
- [ ] Verify `git status --short` is clean and `git log 7298f4b8..HEAD` contains the eight intended commits, including the bounded dependency-audit remediation and final T4 closeout.
