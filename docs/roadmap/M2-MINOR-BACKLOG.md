# M2 minor review findings backlog (64) — deferred, non-blocking

From the M2 build workflow (wf_7f1ccbc7-cca). Triage into M3+ test-hardening.

## Validation findings (orchestrator, post-workflow)
- **integration test isolation** — Running the full `-m integration` suite
  together fails `tests/migrations/test_0001_m1_baseline.py` with
  `pk_roles already exists`: the M2 topology integration tests
  (`tests/engines/topology/`) seed Postgres via `Base.metadata.create_all`
  and leave the schema in place, so the migration test's clean-DB
  `alembic upgrade -> 0001` collides. Each test passes in isolation.
  _Fix:_ DROP/CREATE the `public` schema in the topology integration teardown
  (or a session fixture), so the integration suite is order-independent.
  Functional M2 code is unaffected (3 topology integration tests pass clean).

## M2-01
- **backend/app/knowledge/neo4j_client.py:163** — health_check() swallows OSError (network-level failure) silently with only a warning log. The spec says health_check returns bool without raising — this is implemented correctly — but OSError is a very broad catch that includes programmer errors like TypeError subclassing OSError on some platforms. The standard neo4j driver surface raises ServiceUnavailable (subclass of Neo4jError) and DriverError for connectivity failures; catching raw OSError is unnecessary and could mask unrelated bugs. _Fix:_ Remove OSError from the except tuple in health_check(). ServiceUnavailable (covered by Neo4jError) and DriverError already cover all expected connectivity failures from the neo4j driver.
- **backend/tests/knowledge/test_neo4j_client.py:305** — test_get_client_returns_cached_singleton calls the real get_client() which invokes get_settings() internally. Although the driver is lazy (no I/O), get_settings() is re-invoked after _clear_settings_cache clears the lru_cache, meaning the test depends on a valid ambient Settings being constructable from the environment. If Settings construction fails (missing required env vars in a clean CI environment), this test errors rather than passes. All other tests in this file receive the settings fixture and inject the driver — this singleton test bypasses that isolation. _Fix:_ Monkeypatch get_settings (or directly set _client to a pre-built client) before calling get_client(), so the test is fully self-contained and does not depend on environment-supplied settings construction.
- **/backend/app/knowledge/neo4j_client.py:78** — close() does not null out _driver when driver.close() raises. The lines are `await self._driver.close()` followed unconditionally by `self._driver = None`, but if the await raises an exception the assignment is never reached. The docstring promises 'safe to call repeatedly', but a failed close leaves _driver populated so the next call attempts to close an already-failed driver again. _Fix:_ Use a try/finally to guarantee the reset: `try: await self._driver.close() finally: self._driver = None`.
- **/backend/pyproject.toml:161** — The app.knowledge forbidden-import contract omits app.db. REPO-STRUCTURE §3.2 row 5 lists the allowed imports for app.knowledge as core/schemas/models/llm only; app.db is not in that list. The linter will not catch a future accidental `from app.db import ...` inside app.knowledge, which would violate the one-way Postgres→Neo4j projection invariant (ADR-0005). _Fix:_ Add `"app.db"` to the forbidden_modules list in the `app.knowledge imports only core/schemas/models/llm` contract.
- **/backend/tests/knowledge/test_neo4j_client.py:305** — test_get_client_returns_cached_singleton calls get_client() without injecting a fake driver or test settings. It relies on the autouse _clear_settings_cache fixture then implicitly calls get_settings() with defaults (bolt://neo4j:7687). The test only verifies object identity, not that the singleton was built from the correct settings, and it leaves a real Neo4jClient in the module-level _client slot for the duration of the test. _Fix:_ Monkeypatch neo4j_client.create_client (or _client) with a client built from the test settings fixture and an injected fake driver, consistent with how test_dispose_client_closes_driver_and_resets already works.
- **/backend/app/knowledge/neo4j_client.py:175** — create_client() ignores the driver_factory and max_attempts/backoff_base_seconds parameters — it always constructs Neo4jClient(settings) with defaults. This means callers who want to tune retry parameters or inject a factory at the singleton level must bypass create_client() entirely. The parallel in app/db.py (create_engine) accepts only settings, which is consistent, but the spec says create_client mirrors db.py; the discrepancy between the injectable constructor and the public factory is undocumented. _Fix:_ Either document that create_client is intentionally a thin public entry point (settings-only) and direct test customization to the constructor, or forward **kwargs to Neo4jClient.

## M2-02
- **backend/app/knowledge/__init__.py** — schema.py constants and ensure_constraints are not re-exported from the package __init__.py, while neo4j_client symbols are. Callers must use the full submodule path app.knowledge.schema. This is an API surface inconsistency, not a spec violation. _Fix:_ Optionally add `from app.knowledge.schema import ensure_constraints, NODE_KEY_PROPERTY, LABEL_DEVICE, ...` (or a wildcard __all__) to __init__.py if the team wants a uniform import surface. Not required by the spec.
- **D:\Multi-Agent workflow\network-infrastructure-ai-platform\backend\tests\knowledge\test_schema.py:162** — test_ensure_constraints_covers_uniqueness_for_all_labels uses substring matching ('label in s and prop in s') to assert coverage. For the VRF/Site pair both sharing key property 'name', the check requires the label string to be present too, so there is no false-positive risk — but the test would silently pass if a statement contained both label strings in its text (e.g., a comment). A more precise assertion would verify the exact constraint name (netops_unique_vrf_name, netops_unique_site_name) or parse the FOR (n:<label>) clause. _Fix:_ Assert the deterministic constraint name instead of substring presence: assert any(f'netops_unique_{label.lower()}_{prop.lower()}' in s for s in stmts), which ties the check to the actual _constraint_name() output and eliminates ambiguity between VRF and Site statements.

## M2-03
- **D:\Multi-Agent workflow\network-infrastructure-ai-platform\backend\tests\api\test_devices.py:373** — test_site_appears_in_list asserts items[0]["site"] == "dc-east-1" but all test devices share hostname "core-sw-01", so the list order among peers is by UUID (insertion order). If the test DB has pre-existing devices from fixtures or test-class ordering shifts this is fragile; any device inserted before this one alphabetically or with a lower UUID would become items[0] instead. _Fix:_ Filter by the created device id or assert any(item["site"] == "dc-east-1" for item in items) to make the assertion order-independent.
- **D:\Multi-Agent workflow\network-infrastructure-ai-platform\backend\tests\api\test_devices.py:415** — test_site_max_length_is_validated only covers DeviceCreate (POST). There is no corresponding test for DeviceUpdate (PATCH {"site": "x"*129}) returning 422. The max_length=128 constraint on DeviceUpdate.site is exercised by production code but not by any test. _Fix:_ Add a test in TestDeviceSiteAssignment that sends PATCH with a 129-character site value and asserts 422.
- **D:\Multi-Agent workflow\network-infrastructure-ai-platform\backend\tests\api\test_devices.py:363** — test_site_appears_in_list asserts items[0]["site"] without verifying the returned item is the device that was just created. If test isolation ever weakens (e.g. class-scoped session, shared fixture state) a stale device without a site value could be index 0 and silently fail the intent of the assertion. _Fix:_ Filter items by the created device's id, or assert any(item["site"] == "dc-east-1" for item in items) to make the test robust to ordering.
- **D:\Multi-Agent workflow\network-infrastructure-ai-platform\backend\app\schemas\devices.py:63** — str_strip_whitespace=True (inherited from model_config on DeviceCreate and DeviceUpdate) means a site value consisting entirely of whitespace (e.g. "   ") strips to the empty string "", which passes max_length validation and gets stored as "" rather than None. This creates a semantically inconsistent state (empty-string site vs. null site) with no test coverage. _Fix:_ Add a Field validator (e.g. min_length=1 when not None, or an AfterValidator that coerces "" to None) so that a whitespace-only site is rejected or normalized to None. This pattern already applies to hostname via min_length=1, but nullable string fields like site lack it.

## M2-04
- **backend/tests/engines/topology/test_nodes.py:276** — TestSiteNodes has a single test covering site=None exclusion but no test for site='' (empty string). The implementation silently excludes '' via the falsy check `if device.site` — correct behavior, but the branch is invisible to the suite. Implementer flagged this deviation from the spec's 'non-null only' wording. _Fix:_ Add a test: make a device with site='' and assert it does not produce a SiteNode, alongside the existing None case. Example: make_device('e', '10.0.0.5', site='') should be absent from derived.sites.
- **backend/app/engines/topology/nodes.py:87** — neo4j_properties passes Python None through unchanged for optional fields (vendor_id, model, site on DeviceNode; mac_address on InterfaceNode). The Neo4j driver behaviour on None values differs between driver versions (some remove the property, some store null). No test asserts the None-field output shape, leaving a silent contract gap for the M2-05/M2-06 MERGE writer. _Fix:_ Either document the None-passthrough as intentional in the method docstring and add a test asserting None values appear in the returned dict, or filter None values out (e.g. `if value is not None`) and add a test for that. Pick one and make it explicit before M2-05 consumes the payload.
- **D:\Multi-Agent workflow\network-infrastructure-ai-platform\backend\tests\engines\topology\test_nodes.py:263** — `TestVrfNodes` only tests the `''` sentinel (global routing table) and named VRFs. It does not test a whitespace-only VRF string (`'   '`), which is exactly the input that triggers the sentinel bypass bug. Similarly, `TestSiteNodes` only covers `None`, not `'   '`. The bug would have been caught immediately by a fixture using `vrf='   '` or `site='   '`. _Fix:_ Add a test case in `TestVrfNodes` passing a route with `vrf='   '` and asserting `derived.vrfs == ()`. Add a parallel test in `TestSiteNodes` with `site='   '` and asserting `derived.sites == ()`.

## M2-05
- **backend/app/engines/topology/nodes.py:222** — route_prefix_network is not listed in nodes.__all__. edges.py imports it directly via 'from app.engines.topology.nodes import route_prefix_network', which crosses module boundaries using a name that the module does not declare as part of its public API. A future refactor that cleans up nodes.__all__ could silently break the import. _Fix:_ Add 'route_prefix_network' to __all__ in nodes.py so the shared helper is explicitly part of the module's public contract.
- **backend/app/engines/topology/edges.py:375** — IN_SUBNET edges are emitted for interfaces whose device_id is absent from known_devices, while HAS_INTERFACE edges for the same interface rows are suppressed. The asymmetry is intentional (both IN_SUBNET endpoints are always projected nodes regardless of device membership) and documented in a code comment, but there is no test asserting this explicit behavior: specifically, that an orphaned interface (device_id not in devices) gets an IN_SUBNET edge but no HAS_INTERFACE edge. _Fix:_ Add a test: create an interface with a device_id not in the devices list, call build_l3_edges, and assert in_subnet is non-empty while has_interface is empty for that interface. This documents the design decision and guards against accidental future filtering.
- **backend/app/engines/topology/edges.py:373** — IN_SUBNET edges are built from ALL interfaces with an ip_address regardless of whether their device_id appears in the supplied `devices` set, while HAS_INTERFACE edges (line 367) are filtered to `known_devices`. This asymmetry means the M2-06 projector will receive Interface->Subnet edges for Interface nodes that have no owning Device and no HAS_INTERFACE edge in the same projection pass. Neither endpoint is a phantom (both exist in Postgres), so this does not violate the no-phantom-nodes invariant, and the docstring acknowledges it. However, the orphaned Interface nodes are confusing for the projector and the graph traversal layer, and no test covers this mixed-filter scenario. _Fix:_ Either (a) consistently apply the `known_devices` filter to IN_SUBNET (add `if row.device_id in known_devices` to the `addressed` comprehension used for IN_SUBNET), matching the HAS_INTERFACE behaviour, or (b) explicitly document this as intentional with a test that asserts an orphaned-device interface still produces an IN_SUBNET edge. Option (a) is simpler and makes the four L3 edge types consistent.
- **backend/app/engines/topology/edges.py:242** — Degenerate self-pair handling (local == remote endpoint) produces a self-loop ConnectedToEdge (a == b). This can occur when a neighbor row reports itself as its own neighbor and both sides fall back to the Device endpoint. The resulting edge has identical a and b endpoints. The spec does not address self-loops and no test covers this path; the M2-06 projector will receive a CONNECTED_TO self-loop, which is semantically meaningless and may confuse graph queries. _Fix:_ Add a guard to skip (continue) when `local == remote` after endpoint resolution, analogous to how L3_ADJACENT excludes self-pairs. Add a test asserting that a self-neighbor row produces zero edges.
- **backend/app/engines/topology/edges.py:196** — Asymmetric interface resolution can produce two CONNECTED_TO edges for one physical link. If core reports Gi0/0<->edge Gi0/0 (both interfaces found -> Interface:X -- Interface:Y) and edge simultaneously reports Gi0/0<->core Te1/0/99 (remote interface not found -> Interface:Y -- Device:core), these resolve to different canonical endpoint tuples and survive dedup as two distinct edges. The spec says 'an adjacency seen from both ends becomes ONE edge'; the implementation achieves this only when endpoint resolution is symmetric. The implementer flagged this explicitly. _Fix:_ For the diff layer (M2-06 or topology_snapshots), document that edge count can exceed physical adjacency count when interface naming is asymmetric, and add a test covering this scenario explicitly so the known limitation is pinned in the test suite rather than only in comments.
- **backend/tests/engines/topology/test_edges.py:486** — L3 builder silently skips interface and route rows whose device_id is absent from the supplied devices set (documented in the docstring), but unlike the L2 builder there is no report counter or returned diagnostic. The test at line 486 verifies routes are skipped but only checks the output is empty — there is no mechanism for callers to know how many rows were dropped, which will complicate operational debugging when a device is missing from the projection run. _Fix:_ Minor polish: consider adding a `DerivedL3Edges.skipped_rows` counter analogous to `L2BuildReport.unresolved_neighbors`, or at minimum add a comment in `build_l3_edges` cross-referencing the L2 report pattern so the M2-06 implementer knows to surface this.

## M2-06
- **backend/tests/engines/topology/test_projector.py:184** — _sweeps() filters on 'DELETE' in cypher, capturing both edge-sweep statements ('DELETE r') and node-sweep statements ('DETACH DELETE n'). This is correct for the current test body, but is semantically overloaded: any future wipe-style statement (e.g. from a full_rebuild called inside a combined test) would also match. The helper is used in project()-only tests so no false positive exists today, but the lack of precision makes tests fragile to future composition. _Fix:_ Split into _edge_sweeps() (filter on 'DELETE r' or absence of 'DETACH') and _node_sweeps() (filter on 'DETACH DELETE' and 'WHERE'), or filter on 'last_projected_at' to scope to stale-sweep statements only.
- **backend/app/engines/topology/projector.py:251** — ROUTES_TO edges are MERGEd strictly by (device, subnet) endpoint pair + rel type. Multiple normalized_routes rows from one device to the same prefix (different protocol, vrf, or next_hop) collapse to one relationship whose props are the last row written in iteration order. The graph loses route multiplicity silently. No test documents or asserts this collapse behavior, so graph consumers have no discoverable contract. _Fix:_ Add a test or inline comment that explicitly asserts/documents the collapse: e.g. a fixture with two routes from DEV1 to the same prefix via different protocols, asserting only one ROUTES_TO relationship exists after projection. This makes the trade-off visible and prevents accidental regression if the MERGE key is widened later.
- **backend/tests/engines/topology/test_projector.py:251** — test_project_node_rows_carry_key_and_full_prop_map only inspects Device rows. No unit test asserts that the IPAddress upsert batch has row['key'] == str(IF1) (a UUID string) rather than row['key'] == '10.0.0.1' (the address). The Cypher-text assertion on line 244 only checks the MERGE clause template, not the actual key value passed as a parameter. If IPAddressNode.key_property were corrected and _node_rows were accidentally changed to use node.key instead of props[key_property], this would silently regress. _Fix:_ Add a test case that fetches the IPAddress upsert batch from client.executed and asserts rows[0]['key'] == str(IF1), mirroring the Device key assertion on line 261.
- **backend/app/engines/topology/projector.py:314** — full_rebuild opens three separate Neo4j sessions across three async-with blocks (wipe session, ensure_constraints session, project session). Between the wipe completing and projection completing, the graph contains zero projected nodes — any concurrent reader sees an empty topology subgraph. This is not an atomicity requirement stated in the M2-06 spec, but it is a gap against the ADR-0005 invariant that 'full rebuild must always work', because a crash between sessions leaves the graph permanently wiped with no auto-recovery path visible to the caller. _Fix:_ Document the non-atomicity explicitly in the full_rebuild docstring so callers know to fence concurrent reads (e.g., with a maintenance-mode flag) during rebuild. As a follow-on, consider wrapping wipe + project in a single explicit write transaction if the graph size permits, or using a shadow-swap pattern.
- **backend/app/engines/topology/projector.py:330** — The stale sweep uses a datetime equality watermark (last_projected_at <> $projected_at). The projected_at value is supplied by the caller and is stored in Neo4j as a Bolt DateTime object. If two successive incremental project() calls are given identical projected_at values (caller bug, clock skew, or test harness reuse), the sweep will not delete anything from the first pass — stale elements survive indefinitely. There is no guard or assertion that projected_at is strictly newer than any previously-projected value. _Fix:_ Add a brief caller-contract note in the project() docstring stating that projected_at must be strictly greater than the projected_at of any prior pass for sweep correctness. Optionally, a debug-level log warning when projected_at equals the most recently seen watermark would help diagnose this in production.
- **backend/tests/engines/topology/test_projector.py:456** — The integration test calls client.session() and then await session.run() directly on the returned object (e.g., line 481: await session.run('MERGE ...')). But Neo4jClient.session() yields an AsyncSession via asynccontextmanager; the integration test uses it as async with client.session() as session — which is correct. However, the single_value helper (line 472) opens a fresh session per call, meaning each count query runs in its own auto-committed transaction with no read-your-own-writes guarantee if called immediately after a write. On a loaded server, a read could return stale data from a replica. This is a test reliability nit rather than a production correctness issue. _Fix:_ Wrap all post-projection read assertions in a single session to get consistent read semantics, or add a brief note that the test assumes a single-node Neo4j (which the compose setup provides, so causal consistency is sufficient).

## M2-07
- **D:\Multi-Agent workflow\network-infrastructure-ai-platform\backend\alembic\versions\0003_m2_topology_snapshots.py:35** — sa.Column("id", sa.Uuid(), nullable=False, default=uuid.uuid4) passes `default` as a Python-side kwarg to op.create_table(). Alembic DDL ignores Python-side defaults in migrations — no server_default is emitted for the id column, so raw-SQL inserts would fail without an explicit id. The ORM path (UuidPkMixin) generates the UUID app-side, so normal usage is unaffected. Consistent with the rest of the codebase convention but worth noting. _Fix:_ Either remove the ineffective `default=uuid.uuid4` kwarg (it does nothing in the DDL context) or add `server_default=sa.text("gen_random_uuid()")` for PostgreSQL guard. Since the entire codebase relies on app-side UUID generation, removing the kwarg is cleaner.
- **D:\Multi-Agent workflow\network-infrastructure-ai-platform\backend\tests\engines\topology\test_snapshots.py:243** — test_row_has_timestamps does not call session.refresh(row) after flush, so the timestamp assertions pass against the Python-side `default=utcnow` value (set at object construction), not a DB round-trip value. The test does not exercise the UtcDateTime type decorator's result-processing path on SQLite, making it weaker than it appears. _Fix:_ Add `await session.refresh(row)` before the tzinfo assertions to ensure the values are loaded from the database, actually exercising the UtcDateTime round-trip.
- **D:\Multi-Agent workflow\network-infrastructure-ai-platform\backend\tests\engines\topology\test_snapshots.py:30** — The module-level _ENGINE and _SessionLocal are shared across all test functions. The session fixture calls create_all/drop_all on each test, but concurrent or parametrized runs could collide on the same in-memory database handle. Other test modules in the repo use a per-test engine inside the fixture; this module deviates from that pattern. _Fix:_ Move engine/session-factory creation inside the session fixture (create a fresh engine per test invocation) to match the discovery persistence test pattern and ensure full isolation.
- **D:/Multi-Agent workflow/network-infrastructure-ai-platform/backend/alembic/versions/0003_m2_topology_snapshots.py:35** — Convention drift: `sa.Column("id", sa.Uuid(), nullable=False, default=uuid.uuid4)` passes a Python-side `default=` to a DDL column inside `op.create_table()`. Alembic silently ignores `default=` in this context — no server_default is emitted. The baseline migration (0001) correctly omits `default=` on all DDL `id` columns because UUID generation is ORM-side only (UuidPkMixin). The `import uuid` at the top of the migration exists solely for this unused call. _Fix:_ Remove `default=uuid.uuid4` from the `id` column declaration and remove the `import uuid` line. Match the pattern in 0001: `sa.Column("id", sa.Uuid(), nullable=False)`.
- **D:/Multi-Agent workflow/network-infrastructure-ai-platform/backend/alembic/versions/0003_m2_topology_snapshots.py:53** — Convention drift: `UniqueConstraint` uses a bare string name `"uq_topology_snapshots_run_id"` instead of `name=op.f("uq_topology_snapshots_run_id")`. All seven UniqueConstraints in the baseline migration (0001) use `name=op.f(...)` consistently. _Fix:_ Change to `sa.UniqueConstraint("run_id", name=op.f("uq_topology_snapshots_run_id"))` to match the established pattern.

## M2-08
- **D:\Multi-Agent workflow\network-infrastructure-ai-platform\backend\app\engines\topology\diff.py:35** — NodeElement and EdgeElement are plain type aliases (list[str]) rather than NewType or TypeAlias annotations. They appear in TopologyDiff's field types but are not enforced at runtime and carry no semantic distinction from a bare list[str]. This is a documentation/typing polish issue and does not affect correctness. _Fix:_ Add 'from typing import TypeAlias' (Python 3.10+) or use 'from __future__ import annotations' already present, and annotate as 'NodeElement: TypeAlias = list[str]'. No behavioral change needed.
- **/backend/app/engines/topology/diff.py:67** — _as_tuple_set silently accepts elements of any length. A malformed snapshot with a 3-element 'node' or 2-element 'edge' would not raise; the spurious tuple would appear as a phantom added/removed entry. The canonical build_snapshot contract prevents this today, but the function carries no assertion or length guard, making future misuse silent. _Fix:_ Add a guard or at minimum a comment asserting expected element lengths, e.g. assert all(len(e) == expected_len for e in elements) inside _as_tuple_set, or document the invariant explicitly so future callers know the input contract is not validated.
- **/backend/app/engines/topology/diff.py:35** — NodeElement and EdgeElement are plain list[str] aliases — they carry no arity information in the type system. A node element is always length-2 and an edge element is always length-3, but the type alias does not encode this. This is fine for now, but if Pydantic strict mode or a type checker is applied later the fields will not catch length violations. _Fix:_ Consider annotating as tuple[str, str] and tuple[str, str, str] respectively, matching the internal tuple representation used in _as_tuple_set, or document the invariant in the field docstrings. Low urgency — raise at M2-10 schema review if a strict serializer is introduced.

## M2-09
- **D:\Multi-Agent workflow\network-infrastructure-ai-platform\backend\tests\workers\test_topology_tasks.py:460** — test_rebuild_cli_main_parses_run_id only exercises the happy path (main returns 0). There is no test asserting that main() propagates a rebuild() exception as a non-zero exit. The spec requires rebuild flow tests and explicitly states failures surface as non-zero exit. _Fix:_ Add a test that monkeypatches rebuild to raise, then calls rebuild_mod.main([]) inside pytest.raises(SystemExit) and asserts the exit code is non-zero (e.g. code != 0).
- **D:\Multi-Agent workflow\network-infrastructure-ai-platform\backend\tests\workers\test_discovery_tasks.py:428** — test_topology_sync_enqueued_on_success_not_on_failure does not cover the PARTIAL status case. The spec (and the implementer's own uncertainty flag) leaves PARTIAL trigger open to question; a test asserting PARTIAL also enqueues the sync would close that gap. _Fix:_ Add a scenario where devices_failed > 0 and devices_succeeded > 0 (PARTIAL run) and assert enqueued is non-empty, confirming PARTIAL triggers the topology sync.
- **D:\Multi-Agent workflow\network-infrastructure-ai-platform\backend\app\workers\tasks\topology.py:200** — The error string stored in run.stats['topology_sync']['error'] is f"{type(exc).__name__}: {exc}", which includes str(exc). Connection errors from the neo4j driver or SQLAlchemy can embed the DSN/URI including credentials (e.g. bolt://neo4j:password@host:7687). The secret-free test only checks for the literal strings 'password' and 'neo4j_user', so a real credential in a URL would pass that test undetected. _Fix:_ Store only type(exc).__name__ (or a fixed sanitised message) in the stats dict. Log the full exception at warning level via structlog (structlog already suppresses credential fields if configured), but keep the persisted error string credential-free: sync_stats = {"ok": False, "error": type(exc).__name__}.
- **D:\Multi-Agent workflow\network-infrastructure-ai-platform\backend\tests\workers\test_topology_tasks.py:375** — test_sync_after_run_isolates_projection_failure verifies that no snapshot is written when Neo4j fails, but it does not assert that _record_sync_stats still writes the topology_sync block to run.stats when the projection phase raises. The run.stats check on line 393 passes only because FakeClient(fail=True) raises before upsert_snapshot, yet _record_sync_stats runs in a separate asyncio.run() call — if that second call also failed (different engine, session issue) the stats block would be missing and the test would still pass due to missing assertion coverage on the stats path under failure. _Fix:_ Add an explicit assertion that run.stats['topology_sync']['ok'] is False and run.stats['topology_sync'] exists after a projection failure, confirming the stat-recording path works end-to-end under the same fake-DB fixture. The assertion at line 393 already does this — add a complementary assertion that the 'error' key is present and non-empty to make the test self-documenting.

## M2-10
- **backend/tests/api/test_topology.py:63** — The fake _FakeTx.run re-implements the site/vrf WHERE predicates in Python rather than exercising the real Cypher, so the filter predicates themselves are effectively tested against a duplicate of the production logic (implementer flagged this). The layer selection, node-dedup, projected_at, and coercion paths are genuinely exercised, but the Cypher site/vrf filtering is not. _Fix:_ Add an @pytest.mark.integration test that runs fetch_graph against a real (containerized) Neo4j with seeded nodes/edges to validate the actual Cypher site/vrf predicates, hardening beyond the in-process fake.
- **backend/tests/api/test_topology.py:465** — The diff endpoint has an unauthenticated-401 probe but no inactive-user RBAC probe, whereas the graph endpoint (test_inactive_user_is_401) does. Minor asymmetry in the RBAC probe coverage the spec calls for. _Fix:_ Add a test asserting GET /api/v1/topology/diff with an inactive user's token returns 401, mirroring TestGraph.test_inactive_user_is_401.
- **backend/app/knowledge/topology_read.py:121** — _projected_at uses lexicographic max() over last_projected_at strings to pick the most recent stamp. This equals chronological max only because every stamp is an ISO-8601 string written with the same +00:00 UTC offset and identical formatting by the projector. The dependency on uniform formatting is implicit and brittle if a future projector path ever emits a different offset/precision. _Fix:_ Add a short comment documenting that the lexicographic max is valid only because all last_projected_at values are tz-aware UTC ISO-8601 with a fixed format, or parse to datetime before comparing for robustness.
- **backend/app/knowledge/topology_read.py:22** — The module docstring claims the `site` filter scopes "the interfaces/addresses/subnets reachable from them," but the Cypher predicate is `a.site = $site OR b.site = $site` and only Device nodes carry a `site` property (confirmed in engines/topology/nodes.py: Interface/Subnet/IPAddress nodes have no `site`). Under a site filter, IN_SUBNET edges (Interface->Subnet, neither endpoint has `site`) are silently dropped entirely, so interface/subnet structure is NOT included as the docstring implies. HAS_INTERFACE and ROUTES_TO survive only via their Device endpoint. Behavior satisfies the spec ("site filters scope the subgraph") but the docstring overstates it. _Fix:_ Reword the Filtering section to state that the site filter retains only edges with at least one Device endpoint assigned that site (Interface/Subnet nodes carry no site), so pure Interface<->Subnet (IN_SUBNET) edges are excluded under a site filter. Alternatively, if interface/subnet inclusion is desired, broaden the Cypher to pull device-anchored neighbors explicitly.
- **backend/app/knowledge/topology_read.py:128** — `_projected_at` returns `max(stamps)` over ISO-8601 strings (lexicographic). This equals chronological max only while every `last_projected_at` shares the same UTC offset (`+00:00`), which the ADR-0005 invariant guarantees. It is a silent correctness hazard if a non-UTC-offset stamp ever reaches the graph, since lexicographic ordering of mixed-offset ISO strings is not chronological. _Fix:_ Optional hardening: parse stamps via datetime.fromisoformat and take the chronological max, or add a comment pinning the assumption to the ADR-0005 tz-aware-UTC invariant so the lexicographic shortcut is intentional and documented.

## M2-11
- **backend/tests/engines/topology/test_rebuild_exit_criteria.py:256** — The node-key coalesce expression `coalesce(n.pg_id, n.cidr, toString(n.vlan_id), n.name)` is evaluated against ALL projected labels in one query. It is correct today because the 7 labels form non-overlapping coalesce paths (pg_id for Device/Interface/IPAddress, cidr for Subnet, vlan_id for Vlan, name for VRF/Site). However, `InterfaceNode` stores a `name` field in its Neo4j property map (nodes.py L128) and `DeviceNode` stores `hostname` — these are shadowed by `pg_id` appearing first. If a future label introduces a `cidr` property alongside a `pg_id`, the coalesce would silently use `pg_id` instead of `cidr`, producing a wrong key without any test failure. The implementer flagged this as an uncertainty. The fragility is latent, not currently broken. _Fix:_ Consider extracting the key using the label-specific key property instead of a single coalesce: e.g., `CASE labels(n)[0] WHEN 'Device' THEN n.pg_id WHEN 'Interface' THEN n.pg_id ... END`. Or add a comment cross-referencing NODE_KEY_PROPERTY and asserting the coalesce ordering assumption so a future maintainer knows to update it.
- **backend/tests/engines/topology/test_rebuild_exit_criteria.py:197** — `_postgres_reachable` creates a dedicated `NullPool` engine that connects and immediately disposes — only to have the caller create a second engine for actual use. Two separate TCP connections are made to Postgres in sequence with no logical need for the separation. This is a harmless double-connect but slightly wasteful and the NullPool timeout probe engine is disposed before the test's main engine is created, so there is no resource leak. _Fix:_ Minor style: fold the reachability check into the test body using the main engine, or accept as-is since the overhead is negligible for an integration test.
- **backend/tests/engines/topology/test_rebuild_exit_criteria.py:41** — The module docstring instructs operators to uncomment `ports:` lines in `deploy/docker/docker-compose.yml` to expose Postgres and Neo4j. The compose file ships with those lines commented out by default (confirmed by the implementer). There is no pointer to which specific service stanzas need editing, which could slow down a first-time runner. _Fix:_ Minor doc nit: mention the service names explicitly (e.g., 'uncomment the `ports:` block under the `postgres` and `neo4j` service stanzas') or add a dedicated `docker-compose.integration.yml` override file.
- **backend/tests/engines/topology/test_rebuild_exit_criteria.py:197** — _postgres_reachable creates a NullPool engine with connect_args={"timeout": 3} but never sets pool_pre_ping or any connect timeout on the engine itself — the timeout applies to the asyncpg connection-level timeout (asyncpg.connect's timeout kwarg), which is correct and functional. However the engine is created and disposed inside this probe function but the test's main engine (created on line 309 via db.create_engine) uses the default pool with pool_pre_ping=True and no connect timeout, so a hung Postgres during the test body (after the probe) could stall indefinitely. This is acceptable for integration tests but worth documenting. _Fix:_ Pass connect_args={"timeout": 3} (or a reasonable value) to db.create_engine calls in both tests for consistent timeout behaviour, or accept the current behaviour as-is since integration tests are manually invoked.
- **backend/tests/engines/topology/test_rebuild_exit_criteria.py:309** — Both test functions call db.create_engine(settings) and db.create_sessionmaker(engine) directly. These are the same factory functions used by rebuild() internally — rebuild() also calls db.create_engine(settings) on line 61 of rebuild.py, creating a second connection pool for the same database in the same event loop. This means each test drives three engine instances simultaneously (one for create_all/seed, one inside rebuild() call 1, one inside rebuild() call 2). All are properly disposed, but it is a resource-heavy pattern. No correctness bug, but the test could instead use a single shared engine passed into a patched rebuild() to reduce pool overhead. _Fix:_ Minor: acceptable for integration tests. For future test hardening, consider refactoring rebuild() to accept an optional pre-built engine/client rather than always constructing its own.
- **backend/tests/engines/topology/test_rebuild_exit_criteria.py:394** — _pg_ids helper accepts session: Any typed as Any rather than the neo4j AsyncSession type. This is intentional given the test module uses Any broadly for Neo4j session handles (the neo4j driver's AsyncSession is not the same as SQLAlchemy's AsyncSession), but the function name and parameter name 'session' are ambiguous — it is called with the Neo4j session, not a Postgres session, which could cause confusion for future editors. _Fix:_ Rename the parameter to neo4j_session or annotate with the neo4j type (neo4j.AsyncSession) and add a brief docstring clarifying it is a Neo4j session.
- **backend/tests/engines/topology/test_rebuild_exit_criteria.py:29** — Module docstring instructs operators to run 'docker compose -f deploy/docker/docker-compose.yml up -d postgres neo4j' but does not mention that the compose file's ports: lines must be uncommented first — this is mentioned only parenthetically later. A first-time reader following the Quick Start block verbatim will bring up the services without published ports and then fail to connect. The NETOPS_DATABASE_URL uses 127.0.0.1 which requires host-published ports. _Fix:_ Move the ports-uncomment instruction before the docker compose command in the docstring, or provide the compose override command (docker compose ... -f override.yml) as a self-contained runnable block.

## M2-12
- **frontend/src/api/topology.ts:135** — getTopologyDiff accepts plain string arguments for fromRun/toRun but the backend validates these as UUIDs (422 on invalid format). There is no client-side type narrowing (e.g. a branded UUIDString type or even a runtime format guard) to surface malformed IDs early. Callers passing arbitrary strings will get a raw 422 ApiError with no guidance. This mirrors the same pattern in discovery.ts so it is consistent with the existing convention, but topology diff is the first endpoint where invalid UUIDs have a named, documented 422 path. _Fix:_ Minor polish only — not a blocker. Consider a branded type alias (e.g. `type UUID = string & { readonly _brand: 'UUID' }`) shared across the frontend API layer, or at minimum document the UUID requirement in the JSDoc @param lines (the diff endpoint JSDoc already mentions '(UUID string)' so this is already partially done).
- **frontend/src/__tests__/topology.test.ts:127** — The 'hits the canonical path' test for getTopologyGraph uses stringContaining('/api/v1/topology/graph') which will also match any future path that happens to contain that substring (e.g. /api/v1/topology/graph-extra). The existing devices tests use the same loose pattern, so this is a convention carry-over rather than a new regression, but the assertion could be tighter. _Fix:_ Use toContain or an exact URL check: expect(url).toMatch(/\/api\/v1\/topology\/graph(\?.*)?$/) to anchor to the end of the path segment.
- **frontend/src/__tests__/topology.test.ts:139** — The 'omits query string when no filters supplied' test calls getTopologyGraph({}) but does not also test getTopologyGraph() (no argument) — the default-parameter path. These are functionally equivalent but a test explicitly calling getTopologyGraph() with zero args would confirm the default export contract is correct. _Fix:_ Add a one-line companion assertion or it-block: `await getTopologyGraph(); expect(url).not.toContain('?');` — low priority given the default value is trivially `{}`.

## M2-13
- **D:/Multi-Agent workflow/network-infrastructure-ai-platform/frontend/src/__tests__/TopologyPage.test.tsx:3** — The test file header and describe block comments claim loading/error/empty states are covered, but no test asserts the loading state (the role='status' paragraph rendered while isPending=true). The loading state is implemented correctly in TopologyPage.tsx but is entirely absent from the test suite — 15 tests cover data mapping, layer toggle, detail panel, error, and empty, but not loading. _Fix:_ Add a test that stubs fetch with a promise that never resolves (or resolves after a delay), renders the page, and asserts screen.getByRole('status') with text matching /Loading topology/.
- **D:/Multi-Agent workflow/network-infrastructure-ai-platform/frontend/src/__tests__/TopologyPage.test.tsx:42** — The mock cy.on implementation is declared as on(event, selector, handler) with three parameters. The real TopologyCanvas code also calls cy.on('tap', backgroundHandler) with two arguments (line 150 of TopologyPage.tsx). In the two-arg call JavaScript binds selector=backgroundHandler and handler=undefined, so handlers.push receives { selector: <function>, handler: undefined }. This doesn't throw and does not affect any current test, but if a future test tries to exercise background-tap deselect via the handlers array it will silently find a malformed entry instead of the real handler. _Fix:_ Update the mock's on method to handle both arities: if handler is undefined treat selector as the handler and push with selector='*' (or skip pushing entirely for the non-node case).
- **frontend/src/__tests__/TopologyPage.test.tsx:129** — `EMPTY_GRAPH` has `projected_at: null`. There is no test asserting that the 'as of' timestamp badge is absent when `projected_at` is null. The badge renders conditionally on `data?.projected_at` being truthy, so the null branch is correct code but untested. _Fix:_ Add a test: after `fetchGraph(EMPTY_GRAPH)` resolves and the empty-state is visible, assert `screen.queryByTestId('topology-as-of')` is null.
- **frontend/src/pages/TopologyPage.tsx:44** — `buildStylesheet()` is a pure function with no closures over component state, but it is called inside the `useEffect` on every cy instance rebuild, allocating a new array and style objects each time. It should be hoisted to module scope so it is computed once. _Fix:_ Move `const STYLESHEET = buildStylesheet()` (or inline the array) to module scope outside any function and reference it directly in the `useEffect`.

## M2-14
- **D:/Multi-Agent workflow/network-infrastructure-ai-platform/frontend/src/pages/TopologyPage.tsx** — DiffControls uses a static queryKey ["discovery-runs", "topology-diff"] for the runs list. If a new discovery run completes while the user is on the topology page, the selector will not pick it up without a manual page refresh. This is not a spec violation (the spec only requires reusing the runs listing, not auto-refreshing it), but it differs from the discovery page pattern where staleTime/refetch intervals are typically configured. _Fix:_ Add a short staleTime or a refetchInterval to the useQuery call in DiffControls, or at minimum document that the dropdown is a snapshot-at-mount of the runs list.
- **D:/Multi-Agent workflow/network-infrastructure-ai-platform/frontend/src/__tests__/TopologyPage.test.tsx** — The test 'renders the run-pair selector from the discovery runs list' asserts only that the selects are in the document (which they always are — they are rendered unconditionally), rather than asserting that the run options fetched from the API are visible in the select. The meaningful assertion (option count > 1) is made inside waitFor but then the test duplicates redundant toBeInTheDocument checks that add no coverage. _Fix:_ Remove the redundant toBeInTheDocument assertions at the end of that test case; the waitFor assertion on querySelectorAll option count is the real coverage and is sufficient.
- **docs/roadmap/MVP.md:141** — The 'Disconnecting a lab link and re-discovering' exit criterion is marked [x] complete, but the criterion text describes a live end-to-end action. The inline annotation correctly qualifies it ('end-to-end live link-pull remains deferred to lab validation'), but the checkbox itself signals full completion to anyone reading the roadmap at a glance. This is a doc accuracy nit, not a correctness bug. _Fix:_ Change the checkbox to a partial marker (e.g., `- [~]`) or rephrase the criterion to split the unit-testable sub-criterion from the lab sub-criterion, so the checkbox only covers what M2-14 actually automated.
- **backend/tests/engines/topology/test_m2_exit_criteria.py:46** — The test imports private helpers `_node_rows` and `_node_sets` directly from `app.engines.topology.projector`. These are module-private (underscore prefix) and any internal refactor of the projector will silently break the test without touching the public API. The M1-18 exit-criteria tests avoided this pattern. _Fix:_ Either promote `_node_rows` / `_node_sets` to a `_testing` sub-module or add a thin public wrapper specifically for test introspection. As a minimum, add a comment in the test noting the intentional private import so a future refactor does not miss it.
- **frontend/src/pages/TopologyPage.tsx:230** — `DiffControls` fetches the runs list with `queryKey: ["discovery-runs", "topology-diff"]`. If the DiscoveryPage already caches runs under `["discovery-runs"]` (without the second segment), the two queries will not share the React Query cache, causing a redundant network request when both pages are mounted. No correctness issue, just unnecessary overhead. _Fix:_ Align the query key with whatever key the discovery client uses for the runs list (e.g., `["discovery-runs", { limit: 50 }]`) so the cache entry is shared, or document the intentional separation.
- **frontend/src/__tests__/topology-diff.test.ts:1** — The `diffListItems` ordering contract ('removed before added, edges before nodes') is described in code comments but not directly asserted by any test. The tests verify individual item properties but not the stable sort order. A future reordering of the sort logic would not be caught. _Fix:_ Add one test that calls `diffListItems` with a diff containing both added and removed nodes and edges, and asserts the exact order of the returned array.
