GOAL: Author P5's W0 design gate for the netops platform — six ADRs (0055 cloud credential + normalization model, 0056 AWS plugin incl. Route53, 0057 Azure plugin, 0058 hybrid topology stitching, 0059 durable dispatch: report outbox + send_task ratchet, 0060 scale-certification methodology), the PRODUCTION.md "P5 in progress" marker + Consultant re-check, and the 16 W1–W5 per-task deep specs — implementing docs/roadmap/P5-PLAN.md so every later P5 wave builds against a reviewed contract instead of deciding mid-wave. Headline: Contracted.

**PREFLIGHT — do this before anything else.** At plan-authoring time (2026-07-21) the working tree carried uncommitted modifications that REVERT parts of the merged PR #167 closeout docs (stale pre-merge versions). Branch `docs/p5-w0-adrs` off clean `origin/main` (at or after `33f24074`); leave the dirty tracked files untouched and never commit a tracked-file modification you did not author this round. Verify `git show HEAD:docs/roadmap/PRODUCTION.md | grep -c "P4.*EXIT 2026-07-21"` returns 1 — if 0, P4 has not exited on your base; STOP and report.

**Read first.** Repo root R = `D:/Multi-Agent workflow/network-infrastructure-ai-platform`.

- R/docs/goals/2026-07-21-1620-netops-p5-w0-rider.md — per-ADR content prescriptions, phase plan, named checks.
- R/docs/roadmap/P5-PLAN.md (§0 posture, §1 scope, §3 waves) and R/docs/roadmap/p5-tasks/README.md (per-task goals + exit criteria these specs deepen).
- R/docs/roadmap/PRODUCTION.md §1 P4-exit marker (the P5 inheritance list — the closed scope), §2.5, §2.6, §11; R/docs/roadmap/P4-RELEASE-READINESS.md (exact current report-dispatch guarantee — ADR-0059's starting point).
- Exemplars: R/docs/adr/0050…0053 (ADR shape + status conventions); R/docs/roadmap/p4-tasks/W1-T1-f5-bigip-plugin.md (deep-spec template).

**Posture.** Docs-only round: no product code, no migrations, no dependency changes, no `git push` until told. ADRs land as **Proposed** (flipped only at P5 exit on evidence). Nothing re-decides D1–D16 or any Accepted ADR; ADR-0059 starts from the recorded P4 dispatch guarantee, ADR-0060 stays consistent with ADR-0047's named-ceiling posture. Scope is exactly the P4-exit P5-inheritance list — no telemetry enrichment, no multi-tenancy, no new vendors.

**Phases.** Eleven, in the rider: P1 preflight/branch → P2–P7 one ADR each (0055→0060) → P8 ADR index + cross-citation sweep → P9 PRODUCTION.md marker + Consultant §12 re-check note → P10 the 16 deep specs (W1:3, W2:4, W3:3, W4:3, W5:3) → P11 closeout. Each phase: draft → named checks green (transcript) → one conventional commit ending "(rider PN)".

**Verification** (transcript-provable):
- `ls R/docs/adr/ | grep -E "^005[5-9]|^0060"` shows six new ADR files; `grep -E "0055|0060" R/docs/adr/README.md` shows six index rows marked Proposed / P5 W0.
- `grep -c "P5 in progress" R/docs/roadmap/PRODUCTION.md` = 1, and the marker's scope list matches P5-PLAN.md §1 verbatim (no drift).
- `ls R/docs/roadmap/p5-tasks/W*-T*.md | wc -l` = 16, each spec citing its ADR + PRODUCTION.md section.
- `git log --oneline` shows one commit per phase, none touching the pre-existing dirty tracked files.

**Stop when** all six ADRs, the marker + Consultant note, and the 16 deep specs are committed phase-by-phase with every verification command's output shown green in the transcript — or stop after 25 turns and report exactly what remains.
