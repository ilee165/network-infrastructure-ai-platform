"""Unit tests for the vendor plugin system (app/plugins) and its schemas.

No network, DB, or device I/O: parsing is driven by recorded fixture text in
``tests/plugins/fixtures/`` and transports are in-memory fakes (D16,
REPO-STRUCTURE §5).
"""
