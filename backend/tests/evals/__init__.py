"""P2 W5 evaluation corpora and deterministic scoring suites.

W5-T1 lives here: the labelled firewall-policy-analysis corpus and the
precision/recall scorer that grades the W3 deterministic Security-Agent analysis
service (``app.engines.security.firewall``) against ground-truth labels. See
:mod:`tests.evals.firewall_corpus` for the corpus and
:mod:`tests.evals.test_firewall_analysis_eval` for the scored gate.
"""
