apiVersion: v2
# Throwaway fixture chart for the W1-T1 CNPG render-twice REUSE-STABILITY
# assertion (ci/cnpg/render-twice.sh). NOT shipped — it exists only so CI can
# prove the dev-fallback reuse branch (templates/cloudnativepg-secret.yaml)
# yields a byte-identical superuser/replication PASSWORD when a prior Secret is
# present. `helm template`'s real `lookup` cannot be primed without an API server,
# so the fixture injects the prior Secret data via values and exercises the
# IDENTICAL Sprig reuse idiom (`index $prior.data <key> | b64dec`) the production
# template uses on upgrade.
name: cnpg-reuse-fixture
description: W1-T1 CNPG render-twice reuse-stability fixture (not shipped)
type: application
version: 0.0.0
appVersion: "0"
